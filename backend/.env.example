# ==========================================
# 知识库后端环境配置
# ==========================================

# ------------------------------------------
# 数据库配置
# ------------------------------------------

# 是否使用嵌入式 PostgreSQL（便携版）
# true = 使用 embedded postgres（推荐用于桌面应用）
# false = 使用外部 PostgreSQL 服务器
USE_EMBEDDED_PG=true

# 数据库配置（嵌入式和外部模式共用）
# 嵌入式模式默认值: localhost:15432, 外部模式请修改为实际地址
DATABASE_HOST=localhost
DATABASE_PORT=15432              # 嵌入式使用非标准端口避免冲突，外部数据库通常用 5432
DATABASE_USER=postgres
DATABASE_PASSWORD=postgres
DATABASE_NAME=knowledge_base

# ------------------------------------------
# OpenAI API 配置
# ------------------------------------------
OPENAI_API_KEY=your_openai_api_key_here

# ------------------------------------------
# 服务配置
# ------------------------------------------
DEBUG=false                 # 调试模式
PORT=8000                 # 服务端口
SHOW_LOADING=true         # 是否显示Loading页面
APP_VERSION=0.1.0          # 应用版本号

# 以下为各模块数据目录配置
# - 支持绝对路径
# - 支持相对路径（相对于 backend/ 目录）
# - 留空表示未配置，需要在应用中设置
# 
# 首次使用请在应用中配置路径，或手动下载组件：
# - PostgreSQL: https://www.postgresql.org/download/
# - RustFS: https://github.com/rustfs/rustfs/releases
# - MinerU Models: https://github.com/opendatalab/MinerU
POSTGRES_DIR=                # PostgreSQL 目录，如: backend/postgres
STORE_DIR=                   # 存储服务目录，如: backend/store
MODELS_DIR=                  # AI 模型目录，如: backend/models
# 临时文件目录 - 默认使用用户可写目录（与日志目录同级）
# macOS: ~/Library/Application Support/Uverse/temp/
# Windows: %LOCALAPPDATA%/Uverse/temp/
# Linux: ~/.local/share/Uverse/temp/
TEMP_DIR=

# ------------------------------------------
# 日志配置
# ------------------------------------------
LOG_LEVEL=INFO            # 日志级别: DEBUG, INFO, WARNING, ERROR

# ------------------------------------------
# MinerU PDF 解析配置
# ------------------------------------------
MINERU_BACKEND=pipeline
MINERU_DEVICE=cpu
MINERU_VRAM=1024
MINERU_VIRTUAL_VRAM_SIZE=1
# PDF解析输出目录 - 默认使用用户可写目录（与日志目录同级）
# macOS: ~/Library/Application Support/Uverse/outputs/
# Windows: %LOCALAPPDATA%/Uverse/outputs/
# Linux: ~/.local/share/Uverse/outputs/
MINERU_OUTPUT_DIR=
